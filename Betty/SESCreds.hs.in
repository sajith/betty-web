module Betty.SESCreds (access, secret, sender) where

import Data.String (IsString)

access :: IsString a => a
access = "SES_ACCESS"

secret :: IsString a => a
secret = "SES_SECRET"

sender :: IsString a => a
sender = "SES_SENDER"

